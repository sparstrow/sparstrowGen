# FACTORY-LOOP — superseded

> **📦 SUPERSEDED 2026-07-14 by Factory Workflow v2.**
> This runbook described the page-by-page build era (Design → SPEC.md → office-hours → autoplan →
> routine build → merge). That era is over. The live workflow — the loop, the Definition of Done
> gate, the `staging`/`main` git flow, parallel worktrees, and skill routing — now lives in the
> repo-root **`CLAUDE.md`**, which is the single source of truth every agent follows.
>
> Do not follow the steps that were here. See `CLAUDE.md`. The v2 design rationale is in
> `docs/planned/factory-workflow-v2.md`. This file is kept only as a redirect.
