# Planned

Architecture specs that predate the spec-driven flow — future scope the factory intends to act on.
Distinct from [`docs/archive/`](../archive/) (frozen history, off the read-path) and
[`docs/deferred/`](../deferred/) (parked, with a revival trigger).

Everything here is **live**. `factory-workflow-v2.md` and `curator-office-hours-parity.md` moved to
[`../archive/superseded/`](../archive/superseded/) on 2026-08-01 when the Listener/Curator flow they
describe was retired.

**New work does not land here.** Every artifact for a new unit of work — spec, plan, and tasks —
goes to `specs/<NNN>-<slug>/` at the repo root, written by the spec-kit commands; see `CLAUDE.md`,
Part II. This directory keeps the architecture specs written before that change,
`phase6-hosted-foundation.md` above all.

`docs/specs/` and `docs/plans/` were the artifact home of the superpowers loop and are **frozen as
of 2026-08-05** — they are history too, just newer than this directory.
