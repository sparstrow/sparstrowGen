# docs/specs — FROZEN

> **Frozen on 2026-08-05. Do not add files here.**
>
> New specs live in **`specs/<NNN>-<slug>/spec.md`** at the repo root, written by
> `/speckit.specify`. See `CLAUDE.md`, Part II — The loop.

This directory holds the approved designs of the pre-spec-kit loop, one file per topic, named
`YYYY-MM-DD-<topic>-design.md`. They were written by the superpowers `brainstorming` skill and
committed before planning started.

**They remain accurate as history and as design records.** Read them to understand why something was
built the way it was. Do not treat their process instructions as current — the superpowers loop, the
skill gate, the optional `/plan-ceo-review` / `/plan-eng-review` / `/plan-devex-review` / `/autoplan`
review pass, and the TDD iron law they were written under have all been retired. See
`docs/deferred/2026-08-05-plan-review-lenses.md` for the review lenses specifically.

Architecture specs that predate even this flow live in [`../planned/`](../planned/) and stay there —
those are still live.
