# Bug Reports

Things that are **behaving wrong in the running app** — reported by the owner or
caught by an agent while working. This is different from every other file in
`doc/`:

| File | Holds |
|---|---|
| `KnownGaps.md` | built, but not verified — not a claim that anything is broken |
| `OpenQuestions.md` | needs a decision from the owner |
| **`bug/`** | **something is confirmed or suspected to behave wrong** |
| `security/` | a bug whose impact is specifically a security/trust boundary |

If the wrong behavior is a vulnerability, credential exposure, auth bypass,
injection, or anything else that lets someone do something they shouldn't —
file it in [`../security/`](../security/README.md) instead, using its stricter
format. Everything else — crashes, wrong output, broken flows, UI bugs — goes
here.

## The rule that matters

**Document a bug in the same turn it surfaces — owner-reported or
agent-found — rather than relying on chat history to be re-read.** A bug
mentioned only in a chat message does not exist to the next session.

This applies whether the owner says "this is broken" / "I'm seeing X" / files
any complaint about actual behavior, **or** an agent notices something wrong
while implementing or verifying unrelated work. Either source gets a file,
written before moving on to anything else.

## Format

One file per bug: `BUG-<date>-<slug>.md`, e.g. `BUG-2026-08-16-signup-auto-confirms.md`.

**Copy [`../templates/bug.md`](../templates/bug.md)** — it carries the full
skeleton (Status / Reported by / Reported, then Symptom, Reproduction,
Investigation, Impact, Resolution) with guidance on what belongs in each
section. That template is the canonical format; this file no longer restates
it, so there is only one copy to keep current.

Two things the template will remind you of, worth knowing before you start:
the **Symptom** section takes what you observed, not what you think caused it,
and the file **stays here after it's fixed** — flip Status to 🟢 rather than
deleting it.

## Turning a bug into work

Once a bug is understood well enough to fix, open a task in
[`../tasks/`](../tasks/README.md) (or add it to an existing phase) and link the
task back to the bug file's id. The bug file stays as the historical record;
the task is what actually gets executed and ticked off.

## Index

| ID | Status | Summary |
|---|---|---|
| [`BUG-2026-08-16-pairing-path-wrong-in-cli`](BUG-2026-08-16-pairing-path-wrong-in-cli.md) | 🟢 resolved | `sparstrow pair` sent users to "Settings → Workspace → Runtimes", a tab that doesn't exist. All four strings now name the Machines page (T-M8-04), and that page exists as of T-M8-03 |
| [`BUG-2026-08-16-signup-auto-confirms`](BUG-2026-08-16-signup-auto-confirms.md) | 🟢 resolved | Fresh signup auto-confirmed and auto-logged-in despite "Confirm email" being ON — an `auth.users` trigger was overriding the setting |
| [`BUG-2026-08-18-orphaned-account-rows-on-staging`](BUG-2026-08-18-orphaned-account-rows-on-staging.md) | 🟢 resolved | Staging holds 8 orphaned account trees — profile rows, workspaces and memberships for auth users that no longer exist, unreachable by any RLS policy. `auth.admin.deleteUser` does not cascade |
| [`BUG-2026-08-18-shell-invents-name-from-email`](BUG-2026-08-18-shell-invents-name-from-email.md) | 🟢 resolved | Fixed in T-M10-04: `toSnapshot()` no longer falls back to the email local part; an empty name now stays empty in the shell, matching the row |
| [`BUG-2026-08-20-setup-workspace-error-never-settles`](BUG-2026-08-20-setup-workspace-error-never-settles.md) | 🟢 resolved | `/setup`'s workspace step mounted a form gated by the very query it also observed — a refetch-on-mount feedback loop meant a failed `/workspace` request never settled into the error UI. Latched the loading gate |
| [`BUG-2026-08-20-flaky-realtime-live-events-test`](BUG-2026-08-20-flaky-realtime-live-events-test.md) | 🟢 resolved | `pnpm test` failed intermittently on one `apps/web` test — a cold dynamic `import()` inside the first test body timed out under turbo's five-way parallel run. Fixed by hoisting the import to module scope and dropping the unneeded per-test `vi.resetModules()`; 5/5 full-suite runs clean afterward |
| [`BUG-2026-08-20-remove-machine-doesnt-clear-local-pairing`](BUG-2026-08-20-remove-machine-doesnt-clear-local-pairing.md) | 🟢 resolved | Removing/revoking a machine in the browser only deletes the cloud-side row; the local token on that computer is never cleared, so `sparstrow pair <code>` refuses with "already paired" unless `--force` is added. `machines.md` said plain re-pairing works — it didn't mention `--force` |
| [`BUG-2026-08-22-chat-new-session-404s`](BUG-2026-08-22-chat-new-session-404s.md) | 🟢 resolved | `/chat`'s "Send message" on a new conversation 404s — `POST /api/v1/chat/sessions` has no route at all (real or stub), so the composer shows a bare "The model failed / Not Found". Fixed: built the real cloud handler (chat is cloud-canonical per the schema's own doc comment), mirroring `packages/core`'s daemon-side validation; the adjacent M5 message/retry stubs are untouched |
| [`BUG-2026-08-22-chat-kc-article-overstates-capability`](BUG-2026-08-22-chat-kc-article-overstates-capability.md) | 🟢 resolved | The Chat & Inbox Knowledge Center article claimed chat is "streaming" and "each reply is a run" with cost/provenance tracking — neither is true; sending a message is still a legible M5 stub and chat turns never touch the `runs` table at all. Rewrote the article to describe current behavior only |
| [`BUG-2026-08-22-antigravity-transcript-not-rendered`](BUG-2026-08-22-antigravity-transcript-not-rendered.md) | 🟢 resolved | `/runs/<id>`'s Transcript card showed nothing at all for an `antigravity`-provider run — events genuinely stream and persist durably (cloud/local counts match), but `RunTranscript`'s `EventRow` had no case for the provider's `"raw"` event type and silently dropped it. `claude-code` runs were unaffected |
| [`BUG-2026-08-22-desktop-servicemanager-health-check-times-out`](BUG-2026-08-22-desktop-servicemanager-health-check-times-out.md) | 🟢 resolved | `probeHealth()` sent no auth token, so every probe against the now-authenticated `/system/health` got a 401 forever, indistinguishable from the core being down. Fixed by threading the per-install token (already read by `core-client.ts`) through every `probeHealth()`/shutdown call in `service-manager.ts` |
| [`BUG-2026-08-22-team-create-500-missing-slug`](BUG-2026-08-22-team-create-500-missing-slug.md) | 🟢 resolved | Creating a team, project, or agent 500'd unconditionally — all three tables have a `NOT NULL` `slug` column that neither the client nor the POST handlers ever populated. Fixed in this pass: all three handlers now derive a slug from `name` with a collision retry, matching the existing `workspaces.slug` pattern |
| [`BUG-2026-08-22-teams-page-crashes-with-real-data`](BUG-2026-08-22-teams-page-crashes-with-real-data.md) | 🟢 resolved | `/teams` and `/teams/[teamId]` crashed outright (`Cannot read properties of undefined (reading 'length')`) the instant a real team exists — `GET /teams` and `GET /teams/:id` never joined `team_members`/`team_projects`, so `team.members` was `undefined` against a frontend built on a schema that promises it always exists. Invisible until this session because every prior pass only ever saw the empty state |
| [`BUG-2026-08-22-core-tests-flake-under-turbo-parallelism`](BUG-2026-08-22-core-tests-flake-under-turbo-parallelism.md) | 🟢 resolved | `pnpm test --force` at the repo root intermittently timed out up to 25 `@sparstrow/core` tests across ten unrelated files (graph client/lifecycle/tools, viz-manager, run-manager finalize, git-status, host-fs) — five-way turbo contention starving real child-process/filesystem work of CPU under vitest's tight 5s default. Fixed by bumping `testTimeout`/`hookTimeout` to 20s package-wide, matching a fix one file had already proven for itself |
| [`BUG-2026-08-23-chat-stub-stale-m5-promise`](BUG-2026-08-23-chat-stub-stale-m5-promise.md) | 🟢 resolved | The chat-send/retry and team-manager-chat stubs said "Arriving in M5" — but M5 shipped 2026-08-11/12 and its own phase spec explicitly excludes chat streaming, so the promise was already false. Changed to "not scheduled yet" and pointed at the new spec scoping the real feature |
| [`BUG-2026-08-23-agent-creator-duplicate-user-bubble`](BUG-2026-08-23-agent-creator-duplicate-user-bubble.md) | 🟢 resolved | A fresh Agent Creator interview briefly showed the owner's first message twice (an optimistic bubble plus an early `messages` refetch that already contained the persisted row) — intake 0008's exact race, on the one page that never got `chat-pending.ts`'s fix. Found live while verifying M13, fixed separately: `pendingContent` now drops once `messages` already ends with that same user turn, the content-heuristic sibling of `chat.tsx`'s id-based guard |
| [`BUG-2026-08-23-headless-spawn-skill-leak`](BUG-2026-08-23-headless-spawn-skill-leak.md) | 🟢 resolved (antigravity, live-confirmed) | A headless CLI spawn inherited the operator's full personal `~/.claude` config unisolated; a machine with a preamble-tier personal skill installed could never grant it the tool permission it wanted (no TTY), so antigravity denied every turn immediately with "permission check failed". Fixed with `--disable-slash-commands` on every headless spawn (both providers) and confirmed live — a retried antigravity turn completed. claude-code's own remaining failure on the same machine turned out to be a separate, account-side 401 auth issue, not this bug |
| [`BUG-2026-08-24-claude-browser-pane-reports-hidden-visibility`](BUG-2026-08-24-claude-browser-pane-reports-hidden-visibility.md) | 🟢 resolved (worked around) | The in-app Claude Browser preview pane (`mcp__Claude_Browser__*`) reports `document.visibilityState === "hidden"` on a fresh, foregrounded navigation, starving any page whose data fetching gates on visibility (React Query throughout this app). Not fixed upstream (harness code), but `doc/runbooks/agent-browser-session.md` now prescribes the `agent-browser` CLI instead of the pane/Playwright for the verification walkthrough — verified live to report visibility correctly |
| [`BUG-2026-08-24-hosted-app-never-loads-its-typeface`](BUG-2026-08-24-hosted-app-never-loads-its-typeface.md) | 🟢 resolved | `apps/web` rendered in the browser's fallback sans-serif on every screen — `DESIGN.md`/`globals.css` name Inter Variable, but `layout.tsx` loaded unused Geist/Geist Mono instead and never imported `@fontsource-variable/inter`. Pre-existing since the Next migration, exposed (not caused) by `T-VR-01`. Fixed: `layout.tsx` now imports Inter directly, Geist loaders removed; verified via `getComputedStyle` in a browser |
| [`BUG-2026-08-24-sidebar-nav-has-no-aria-current`](BUG-2026-08-24-sidebar-nav-has-no-aria-current.md) | 🟢 resolved | No sidebar link carried `aria-current="page"` on any route — `app-shell.tsx` computed `isActive` into `className` only. Found during `T-VR-04`'s interim verification pass; fixed by adding the attribute to the sidebar `Link` |
| [`BUG-2026-08-24-knowledge-breadcrumb-title-silently-blank`](BUG-2026-08-24-knowledge-breadcrumb-title-silently-blank.md) | 🟢 resolved | `lib/knowledge.ts` built its article registry with Vite's `import.meta.glob`, which Turbopack silently no-ops rather than errors on — so Knowledge Center breadcrumbs/tab labels showed the raw slug instead of the article title on every route, invisible since `T-VR-01` deleted the Vite host. Fixed in `T-VR-07` by deleting the broken file and threading a `{slug,title}[]` index from the root layout down to the client consumers |
| [`BUG-2026-08-24-project-provision-always-400s`](BUG-2026-08-24-project-provision-always-400s.md) | 🟢 resolved | Every "New project" creation path 400s unconditionally — `POST /api/v1/projects/provision` spread three client-only fields (`mode`, `gitInit`, `gitUrl`) straight into the `projects` insert and never generated a `slug`, the exact gap `BUG-2026-08-22-team-create-500-missing-slug` fixed on the sibling `/projects` handler but not this one. Found live during `T-VR-06`'s verification pass, unrelated to the Vite retirement. Fixed by mirroring that handler's fix; verified live end-to-end through the actual dialog on a second disposable account, plus 5 new unit tests |
| [`BUG-2026-08-24-terminals-article-describes-a-transport-that-no-longer-exists`](BUG-2026-08-24-terminals-article-describes-a-transport-that-no-longer-exists.md) | 🟢 resolved | The Terminals Knowledge Center article described a transport that no longer exists ("streamed over the local WebSocket" — unreachable from the hosted app since `T-VR-01`) and stated the opposite of the machine's real session behaviour ("no detach/reattach"). Found while planning the terminal spec; rewritten by `T-M17-05` against what M16/M17 actually ship |
| [`BUG-2026-08-24-expired-session-turns-a-server-action-into-a-runtime-error`](BUG-2026-08-24-expired-session-turns-a-server-action-into-a-runtime-error.md) | 🟢 resolved | A Server Action submitted after the session expired showed a Next.js "Runtime Error: An unexpected response was received from the server" overlay instead of a message — the middleware redirected the action's POST to `/login`, so the action never ran and React's dispatch got HTML where it expected an action response. The middleware's existing "API routes authenticate themselves" carve-out predates Server Actions; extended it to them (`Next-Action` header), so `actionContext()` refuses with a legible "Not signed in." Found by `T-WA-01`'s walk; would have affected all 21 files band 22 converts |
| [`BUG-2026-08-25-network-failure-during-a-server-action-is-an-unhandled-rejection`](BUG-2026-08-25-network-failure-during-a-server-action-is-an-unhandled-rejection.md) | 🟢 resolved | A dropped connection mid-write showed a full-screen "Runtime TypeError: Failed to fetch" overlay instead of a message — `ActionResult` covers failures an action *returns*, and a transport failure never reaches the action, so the rejection went unhandled inside `useTransition`. React Query's `onError` had covered both. Fixed with `lib/call-action.ts`, wrapping every call site; matters especially in Electron, whose window points at a remote host and whose offline screen only catches failed navigations |
| [`BUG-2026-08-25-project-variants-read-queries-a-table-that-does-not-exist`](BUG-2026-08-25-project-variants-read-queries-a-table-that-does-not-exist.md) | 🟢 resolved | `GET /projects/:id/variants` and `useProjectVariants` queried `project_variants`, a table that never existed — a variant is really a `projects` row with `parentProjectId` set. Found while converting `useCreateVariant` in `T-WA-02` (writes-only scope, so left as found); fixed on `development` (PR #134, picked up via band 22's pre-promotion merge) by querying `projects` filtered on `parent_project_id` instead |
| [`BUG-2026-08-25-skills-list-file-count-is-nan`](BUG-2026-08-25-skills-list-file-count-is-nan.md) | 🟢 resolved | The Skills list page showed "NaN" in the Files column for every skill — `GET /skills` never returned a `fileCount` field. Found live while verifying `T-WA-05`; fixed on `development` (PR #134, picked up via band 22's pre-promotion merge) by removing the Files column from the list rather than teaching the read to aggregate one — the detail page's own file tree still shows an accurate count |
| [`BUG-2026-08-25-skill-detail-page-always-crashes`](BUG-2026-08-25-skill-detail-page-always-crashes.md) | 🟢 resolved | Every skill's detail page crashed with "Cannot read properties of undefined (reading 'map')" — `GET /skills/:id` never joined `skill_files`. Unchanged since M3 (#80); found live while verifying `T-WA-05`, which could not exercise its detail-page actions through the UI as a result. Fixed on `development` (PR #134, picked up via band 22's pre-promotion merge) by joining `skill_files` in the read |
| [`BUG-2026-08-25-attention-queue-rows-always-render-as-ready-for-review`](BUG-2026-08-25-attention-queue-rows-always-render-as-ready-for-review.md) | 🟢 resolved | The dashboard's attention queue rendered every row as a plain "ready for review" link — the handler sent a `kind` field, the client read `type`, never aliased. `QuestionCard`/`ApprovalCard` had apparently never actually mounted. Found live while verifying `T-WA-04`, which verified `answerTaskAction`/`approveTaskAction`/`denyTaskAction` via unit tests instead. Fixed on `development` (PR #134, picked up via band 22's pre-promotion merge) by renaming the handler's field to `type` |
| [`BUG-2026-08-25-goal-detail-500s-once-a-plan-has-nodes`](BUG-2026-08-25-goal-detail-500s-once-a-plan-has-nodes.md) | 🟢 resolved | `GET /goals/:id` 500s for any goal with at least one plan node — `planNodes.taskId` had no FK to `tasks.id`, so PostgREST's embedded-relationship join could never resolve. Unchanged since M3 (#80); found live while verifying `T-WA-04`, which verified `cancelGoalAction`/`retryNodeAction` via unit tests instead. Fixed on `development` (PR #134, picked up via band 22's pre-promotion merge) by adding the missing FK via migration `0007_dear_calypso.sql` |
| [`BUG-2026-08-25-creating-a-pipeline-always-400s`](BUG-2026-08-25-creating-a-pipeline-always-400s.md) | 🔴 open | Submitting "New pipeline" always 400s with "Could not find the 'steps' column of 'pipelines' in the schema cache" — `pipelines` has no `steps` column (they live in a separate `pipeline_steps` table nothing writes to), and the write handler passes the client's `steps` array straight into the `pipelines` insert. Pre-existing; found live while verifying `T-WA-06`'s `createPipelineAction`, which moved the same broken insert verbatim per plan Scope boundaries |
| [`BUG-2026-08-26-chat-session-updates-always-404`](BUG-2026-08-26-chat-session-updates-always-404.md) | 🟢 resolved | Renaming a chat session, switching its model mid-conversation, or archiving it all 404'd — `PATCH /chat/sessions/:id` was never registered as a route at all. Found converting `T-WA-03`'s `agent-create.tsx` (its own archive-after-create call); fixed for that call site via the new `updateChatSessionAction`, then `T-WA-07` converted `chat.tsx`'s three remaining call sites (rename/model-switch/archive) onto the same action — verified live, model switch and archive both persisting |
| [`BUG-2026-08-26-agent-update-always-404s`](BUG-2026-08-26-agent-update-always-404s.md) | 🟢 resolved | Every agent update — the enabled toggle, the full edit form — 404'd: `useUpdateAgent` sent `PUT /agents/:id`, the handler only ever registered `PATCH /agents/:id`. Found and fixed converting `T-WA-03`'s `updateAgentAction` (a direct DB call has no HTTP verb to mismatch); verified live, both persisting across a reload |
| [`BUG-2026-08-26-action-error-mapping-missing-three-codes`](BUG-2026-08-26-action-error-mapping-missing-three-codes.md) | 🟢 resolved | `actionErrorFrom` (the shared helper every WA-phase action calls) only mapped 2 of `handleError`'s 5 Postgres error codes — an RLS denial, a unique violation, or a foreign-key violation on any converted action showed the raw Postgres error text instead of the same friendly message `/api/v1` always gave. Phase-wide gap since `T-WA-01`; found and fixed while checking `T-WA-03`'s `deleteAgentAction`, applies retroactively to every already-converted action |
| [`BUG-2026-08-26-blocked-project-actions-reassign-and-relink-always-404`](BUG-2026-08-26-blocked-project-actions-reassign-and-relink-always-404.md) | 🟢 resolved | Two of the four blocked-task recovery actions (reassign to another machine, clear the blocked state after a relink) silently did nothing — `useUpdateTask` sent `PUT /tasks/:id`, the handler only ever registered `PATCH /tasks/:id`. Same shape as the agent-update bug; `T-WA-04`/`T-WA-06` both kept the hook alive for this one remaining consumer without noticing it was broken. Fixed converting `T-WA-08`'s `blocked-project-actions.tsx` onto `updateTaskAction` (a direct DB call has no verb to mismatch) |
| [`BUG-2026-08-26-system-settings-route-does-not-exist`](BUG-2026-08-26-system-settings-route-does-not-exist.md) | 🔴 open | Settings → Advanced always shows "No settings stored yet" — `GET`/`PUT /system/settings` (`useSettings`/`useUpdateSettings`) are registered nowhere, not even as a stub. `WipSnapshotCard`, the other consumer, is now fully dead code: its `account === null` gate can no longer be true in the single app `D-24` collapsed to. Found converting `T-WA-08`; left unconverted (same exclusion `stubs.ts`-backed hooks get) since there is no real route to move — fixing it needs a product decision (build the route, or delete both cards as dead) this task cannot make |
| [`BUG-2026-08-26-manager-chat-panel-publish-pipeline-always-404`](BUG-2026-08-26-manager-chat-panel-publish-pipeline-always-404.md) | 🟢 resolved | Publishing a pipeline drafted via a team's Manager Chat always 404'd — `T-WA-06` kept `useCreatePipeline` alive for this exact consumer but deleted the `POST /pipelines` route underneath it, since `manager-chat-panel.tsx` was never in that task's file list. Found by `T-WA-09`'s mandated cross-cluster sweep; fixed by converting the call site onto the existing `createPipelineAction` |
| [`BUG-2026-08-27-dashboard-article-describes-a-transport-that-no-longer-exists`](BUG-2026-08-27-dashboard-article-describes-a-transport-that-no-longer-exists.md) | 🔴 open | `dashboard.md`'s "Notes & limitations" says the attention queue count updates "over the local WebSocket" — it's a plain 5s REST poll (`useAttentionQueue`), same stale-transport class as the terminals article. Found while grepping for that exact string during `T-M17-05`'s verification; filed rather than fixed, out of that task's scope |
| [`BUG-2026-08-27-realtime-refresh-never-took-effect`](BUG-2026-08-27-realtime-refresh-never-took-effect.md) | 🟢 resolved | Core's Realtime credential refresh re-minted a token and called `setAuth`, and the client kept presenting the **original** token forever — realtime-js treats a configured `accessToken` callback as the source of truth over `setAuth(token)`, and core's callback closed over the credential minted at connect time. The existing refresh test asserted `setAuth` was called with a token string that was identical on both mints, so it could never have failed. Would have surfaced as "terminals work for an hour, then the machine goes quiet". Found and fixed in `T-DI-04`; both new regression tests verified to fail against the old code |
| [`BUG-2026-08-27-daemon-realtime-token-cannot-pass-terminal-channel-rls`](BUG-2026-08-27-daemon-realtime-token-cannot-pass-terminal-channel-rls.md) | 🟢 resolved | Even with a validly signed token, the daemon's minted Realtime credential had no `sub` claim and so could not pass `018_terminal_channels.sql`'s admin-membership RLS check. Fixed by the `DI` band's real daemon Auth identity (`019`/`020`) + Supabase-signed session (`T-DI-03`); verified live `T-DI-05` 2026-08-28 — the daemon's control channel reaches `SUBSCRIBED` with no `Unauthorized` refusal |
| [`BUG-2026-08-28-realtime-connect-races-channel-subscribe-auth`](BUG-2026-08-28-realtime-connect-races-channel-subscribe-auth.md) | 🟢 resolved | `packages/core`'s `establish()` called `channel.subscribe()` immediately after `realtime.connect()`, which fires its own auth resolution but doesn't await it — the join payload was built with no `access_token` at all, refused by every `to authenticated` policy, on every single connection attempt (not intermittent). Found live running `T-DI-05` against a real paired daemon; fixed with one `await realtime.setAuth()` before subscribing |
| [`BUG-2026-08-28-terminal-channel-sends-before-control-channel-joined`](BUG-2026-08-28-terminal-channel-sends-before-control-channel-joined.md) | 🟢 resolved | `apps/web`'s `ensureControlChannel()` resolved its promise as soon as `channel.subscribe()` was called, not once the join settled — every browser-side terminal request fell back to the slower REST broadcast endpoint instead of the WebSocket push it had already paid to open. Found and fixed alongside the daemon-side race above, same `T-DI-05` pass |
| [`BUG-2026-08-28-private-broadcast-channels-not-relaying`](BUG-2026-08-28-private-broadcast-channels-not-relaying.md) | 🔴 open | A message broadcast on a `private: true` Realtime channel is never delivered to a subscriber on this project — reproduced with two independent, fully-authenticated, both-`SUBSCRIBED` synthetic connections using only pre-existing M16 policies, unrelated to the daemon or this band's new SQL. Public channels relay correctly. "Allow public access to channels" checked and disabled by the owner — ruled out, symptom unchanged. Root-caused further via Supabase's own Realtime Inspector: joining the topic as `postgres` works fully (presence + self-broadcast round-trip); joining as `authenticated` (the real, correctly-RLS-authorized daemon identity) joins successfully but never hears even its own self-sent broadcast. Strongest untried lead: the Realtime Settings "Database connection pool size" (currently `2`) used for authorization checks. Blocks `T-DI-05` and, if it also affects `010`/`015`'s identical pattern, run transcripts and chat turn deltas too |
| [`BUG-2026-08-27-header-badge-shows-offline-with-active-machine`](BUG-2026-08-27-header-badge-shows-offline-with-active-machine.md) | 🔴 open | The header's "live"/"offline" connection badge (`app-shell.tsx`) reads Realtime run/chat channel connectivity, not machine reachability — on `/machines` and any page with no open run/chat channel, it shows a permanent red "offline" even while a paired machine's own tile correctly shows "active" from a separate heartbeat query. Also structurally wrong for multiple paired machines (one global pill can't say which machine is down); redesign direction captured in `doc/Ideas.md` I-15 |
| [`BUG-2026-08-28-chat-sessions-list-ignores-filters`](BUG-2026-08-28-chat-sessions-list-ignores-filters.md) | 🟢 resolved | `GET /chat/sessions` never applied the `kind`/`projectId`/`agentId`/`status` filters the client already sent — every rail filter (including "Archived") was a no-op since M12–M15 shipped this route. Found verifying `T-CS1-02`'s Archive action: the database updated correctly but the session never left the default list. Fixed by adding the missing `.eq(...)` filters, matching the pattern every other filtered list route in this file already uses |
| [`BUG-2026-08-28-antigravity-model-picker-can-get-stuck-stale`](BUG-2026-08-28-antigravity-model-picker-can-get-stuck-stale.md) | 🟢 resolved | The `antigravity` model picker's "no models available yet — checking…" empty state could persist forever — a one-shot `triggeredRef` latch meant no retry ever happened once the first discovery attempt found no online runtime, and a fallback/failed report separately reset the staleness clock the same as a real success. Chat sending was unaffected (separate gate). Fixed with a self-rescheduling retry loop gated on a live-aware trust check; verified live via `agent-browser` — three discovery cycles fired ~30s apart instead of one attempt ever |
| [`BUG-2026-08-28-enqueue-chat-turn-redefinition-drops-auto-title`](BUG-2026-08-28-enqueue-chat-turn-redefinition-drops-auto-title.md) | 🟢 resolved | US2 was silently dead on `band/26`: `022_chat_auto_title.sql` added the auto-title block to `enqueue_chat_turn`, then `024` and `026` each re-created that same function from an **older migration file's** body and dropped it. `create or replace function` is silent about this — nothing errored, no advisor fired, and both later tasks verified their own feature honestly. Caught only by `T-CS6-02` re-walking CS2's acceptance scenario. Fixed by `027_restore_chat_auto_title.sql`; the function's `comment on function` now warns that any replacement must start from the **current database body**, not an older migration file |
| [`BUG-2026-08-28-provider-switch-pins-invalid-model`](BUG-2026-08-28-provider-switch-pins-invalid-model.md) | 🟢 resolved | Switching a session to `antigravity` before its model cache warms pinned `model = "sonnet"` — a claude-code model, so the next turn dispatches `agy --model sonnet` and fails. `T-CS4-01` made a previously-unreachable `?? "sonnet"` fallback live by moving the list to the cache; the draft-side twin fell back to `""`, which `createChatSession`'s own `?? "sonnet"` cannot catch (`""` is not nullish). Both now use `defaultModelForProvider` in `apps/web/src/lib/chat-models.ts`, falling back to the provider's own compiled-in seed, with tests pinning the cold-cache invariant |
| [`BUG-2026-08-28-password-reset-link-lands-on-dashboard`](BUG-2026-08-28-password-reset-link-lands-on-dashboard.md) | 🟢 resolved | An emailed password-reset link signed the user in and dropped them on the dashboard, never showing the new-password form — `safeRedirectPath` strips every `/auth/*` path to `/`, erasing the recovery destination on every reset. Silent and looks like success: the old password stays in force while the user believes they changed it. The exact failure `otp-types.ts` warns about, guarded in `/auth/confirm` but never in `/auth/callback`. Found while fixing the cross-browser PKCE feedback item, whose recovery branch turned out to be unreachable for this reason. Fixed by reading the raw `next` before sanitising and substituting a fixed literal destination |
| [`BUG-2026-08-28-project-chat-cannot-choose-model-at-creation`](BUG-2026-08-28-project-chat-cannot-choose-model-at-creation.md) | 🟢 resolved | Starting a chat about a project offers no Provider/Model picker — they rendered only for `draftKind === "free"` and were dropped on create. Fixed in `CS7`: Provider (`GhostSelect`) and Model (`ModelPicker`) now render and forward for `draftKind === "project"` as well |
| [`BUG-2026-08-29-antigravity-generated-images-not-captured-in-chat-outbox`](BUG-2026-08-29-antigravity-generated-images-not-captured-in-chat-outbox.md) | 🟢 resolved | When asking Antigravity models to generate images, `agy`'s native `generate_image` tool outputs to `~/.gemini/antigravity-cli/brain/<conv_id>/` rather than `outboxDir`. Fixed by adding `harvestAntigravityBrainFiles` before `sweepOutbox` and providing fallback text for tool-only turns. Verified live in browser with image rendering inline and in the Preview panel |
| [`BUG-2026-08-29-bootstrap-workspace-020-reverted-012`](BUG-2026-08-29-bootstrap-workspace-020-reverted-012.md) | 🟢 resolved | `020_bootstrap_refuses_daemon.sql` copied `004`'s pre-`012` function body instead of the live one, silently reverting `012_no_invented_names.sql`'s fix — every workspace/user bootstrapped since 2026-08-28 got an invented name again (`'Personal Workspace'`, email local-part). Same failure class as `BUG-2026-08-28-enqueue-chat-turn-redefinition-drops-auto-title`, found while replaying policy history onto a fresh prod project. Fixed by `028_restore_no_invented_names_after_020_regression.sql`, applied to both staging and prod |
| [`BUG-2026-08-29-missing-migration-files-for-two-live-tables`](BUG-2026-08-29-missing-migration-files-for-two-live-tables.md) | 🟢 resolved | `chat_message_attachments` and `provider_model_cache` exist live on staging but had no `CREATE TABLE` in any of the 8 numbered drizzle migrations — replaying the tracked history onto a fresh database errors with `relation "chat_message_attachments" does not exist`. Found setting up the production project from scratch. Fixed: table created directly on prod matching staging's live definition, plus a proper `drizzle-kit generate`-authored `0008` migration + snapshot so a future fresh setup doesn't hit the same gap |
| [`BUG-2026-08-30-desktop-stable-staging-share-userdata-dir`](BUG-2026-08-30-desktop-stable-staging-share-userdata-dir.md) | 🟢 resolved | `build-channel-config.mjs` gave `stable`/`staging` distinct `appId`/`productName` but never `name` — Electron's `app.getPath("userData")` is keyed off `app.name` (from the packaged `package.json`'s `name` field), not `productName`, so both channels defaulted to the same `...\Roaming\@sparstrow/desktop` userData dir. Confirmed on a real installed-and-launched staging build's process tree before either channel had ever been run side by side. Fixed by adding `extraMetadata.name` per channel (`T-DR-04`) |
