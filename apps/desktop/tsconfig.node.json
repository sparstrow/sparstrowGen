{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "The Electron main process and the preload script. Node globals, no DOM —",
    "except the two DOM types `contextBridge` typings pull in, which is why",
    "preload sits here rather than with the renderer: it runs in a privileged",
    "Node context and only LOOKS like browser code."
  ],
  "extends": "@sparstrow/tsconfig/node-library.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["ES2023", "DOM"],
    "noEmit": true
  },
  "include": ["src/main", "src/preload", "electron.vite.config.ts", "vitest.config.ts", "scripts"]
}
