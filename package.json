{
  "name": "sparstrowgen",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.6.0",
  "description": "Sparstrowgen — local-first agent harness: orchestrate Claude Code / Gemini CLI agents with scoped Obsidian-compatible memory, task board, pipelines, and cron.",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
