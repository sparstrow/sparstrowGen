{
  "name": "sparstrowgen",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.6.0",
  "description": "Sparstrowgen — local-first agent harness: orchestrate Claude Code / Antigravity CLI agents with scoped Obsidian-compatible memory, task board, pipelines, and cron.",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev:preview:core": "node scripts/dev-preview.mjs core",
    "dev:preview:ui": "node scripts/dev-preview.mjs ui"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "postgres": "^3.4.9"
  }
}
